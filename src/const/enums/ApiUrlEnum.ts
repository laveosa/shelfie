export enum ApiUrlEnum {
  BASE_URL = "https://dummyjson.com", //TODO delete when we will have real base API url
  PRODUCTS = "/products",
  USERS = "/users",
  COMPANIES = "/companies",
  LOGS = "/logs",
}
